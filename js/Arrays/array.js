// масиви
const friends = ["Tima", "Artem", "Mark", "Vlad"]
console.log(friends)
const meFriend = friends[1]
console.log(meFriend)
friends[4] = "Olga"
console.log(friends)
const friendsEqual = friends.length
console.log(friendsEqual)
const lastIndex = friends.length - 1
console.log(lastIndex)
const lastElement = friends[lastIndex]
console.log(lastElement)
// масиви чисел
const numbers = [1, 2, 3, 4, 5]
let total = 0;
for (let i = 0; i < numbers.length; i++) {
total += numbers[i]
console.log(numbers[i])
}

